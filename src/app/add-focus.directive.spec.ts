import { AddFocusDirective } from './add-focus.directive';

describe('AddFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AddFocusDirective();
    expect(directive).toBeTruthy();
  });
});
